/**
 * Bud file for lint
 */

'use strict'

const {eslintrc, eslintignore, prettierrc} = require('the-templates')

module.exports = [
  eslintrc(),
  eslintignore(),
  prettierrc(),
]

if (!module.parent) {
  require('coz').render(__filename)
}
