/**
 * Test for client.
 * Runs with mocha.
 */
'use strict'

const client = require('..')
const { ok, equal } = require('assert')

describe('client', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
